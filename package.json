{
  "name": "dr-alisson-fortes-landing-page",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite --port ${PORT:-3000} --strictPort",
    "start": "vite --port ${PORT:-3000} --strictPort",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
